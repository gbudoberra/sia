import json

import numpy as np


def write_list_to_file(matrix, file_name):
    # diccionario = { "n_layers": len(matrix)}
    # for layer, i in zip(matrix, range(len(matrix))):
    #     diccionario[str(i)] = layer
    #
    # with open(file_name, 'w') as archivo:
    #     json.dump(diccionario, archivo)
    return


# def read_list_from_file(file_name):
#     return np.loadtxt(file_name)
